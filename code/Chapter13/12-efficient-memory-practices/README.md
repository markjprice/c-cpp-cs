# Efficient memory practices

Combines several chapter practices: bounded collections, disposal, caller-provided buffers, and avoiding unnecessary graph retention.

## Build and run

```bash
dotnet build
dotnet run
```

The project targets `net11.0` by default. If you are using .NET 10, change the target framework in the `.csproj` file to `net10.0`.
