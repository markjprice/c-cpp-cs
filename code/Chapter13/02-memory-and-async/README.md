# Memory and async APIs

Shows why Memory<T> and ReadOnlyMemory<T> are used when data must be stored or passed to asynchronous APIs.

## Build and run

```bash
dotnet build
dotnet run
```

The project targets `net11.0` by default. If you are using .NET 10, change the target framework in the `.csproj` file to `net10.0`.
