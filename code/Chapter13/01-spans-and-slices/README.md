# Spans and slices

Shows how spans provide views over existing arrays and strings without copying, while range expressions can create new arrays or strings.

## Build and run

```bash
dotnet build
dotnet run
```

The project targets `net11.0` by default. If you are using .NET 10, change the target framework in the `.csproj` file to `net10.0`.
