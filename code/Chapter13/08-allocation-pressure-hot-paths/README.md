# Allocation pressure and hot paths

Compares readable string/LINQ examples with lower-allocation alternatives that may be justified in repeated hot paths.

## Build and run

```bash
dotnet build
dotnet run
```

The project targets `net11.0` by default. If you are using .NET 10, change the target framework in the `.csproj` file to `net10.0`.
