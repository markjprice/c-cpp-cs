# Delegates, lambdas, and higher-order functions

Build and run this project from this folder:

```bash
dotnet build
dotnet run
```

This project targets `net11.0` and enables nullable reference types.

