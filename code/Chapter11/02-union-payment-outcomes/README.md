# C# 15 union types with payment outcomes

Build and run this project from this folder:

```bash
dotnet build
dotnet run
```

This project targets `net11.0` and enables nullable reference types.

This project uses the C# 15 `union` keyword. It requires a compiler that supports union types. If your SDK does not yet support this feature, the build will fail until you install a compatible SDK.

