**Prompts for *Chapter 15 Building the Puzzle Arcade Shell***

- [Understanding games as state machines](#understanding-games-as-state-machines)
- [Introducing the Puzzle Arcade project](#introducing-the-puzzle-arcade-project)
- [Creating the shared Blazor game shell](#creating-the-shared-blazor-game-shell)
- [Modeling game state with C# 15 union types](#modeling-game-state-with-c-15-union-types)
- [Modeling player actions](#modeling-player-actions)
- [Connecting Blazor UI events to game logic](#connecting-blazor-ui-events-to-game-logic)
- [Building a shared game status and command bar](#building-a-shared-game-status-and-command-bar)


# Understanding games as state machines

```text
Please explain the idea of a state machine using a simple browser puzzle game. Use the states NotStarted, Playing, Won, and Lost, and show which player actions might move the game between those states. Compare using four Boolean fields such as isStarted, isPlaying, hasWon, and hasLost with using one GameState value. Why can separate Boolean fields create impossible combinations?
```

```text
Please explain why the UI should be a projection of game state rather than the place where the game rules live. Use examples from a Blazor puzzle game.
```

```text
In a Blazor app, why is it useful for event handlers to be small methods that create actions and pass them to game logic?
```

```text
Explain how the same state-machine pattern used in a simple puzzle game could apply to a shopping cart, checkout flow, login flow, or wizard-style form.
```

# Introducing the Puzzle Arcade project

```text
Create a beginner-friendly analogy that explains the relationship between game state, player actions, rules, and UI rendering.
```

# Creating the shared Blazor game shell

```text
Please explain how Blazor WebAssembly lets C# code run in the browser, and why that makes it suitable for a small puzzle arcade with no database or server-side game state.
```

# Modeling game state with C# 15 union types

```text
Compare an enum, an interface, a base class, and a union type for modeling the lifecycle of a game. When would each option be a good or bad choice?
```

# Modeling player actions

```text
Please explain why a PuzzleAction union is better than passing around strings such as "start", "guess", "reset", and "hint". And explain why each union case should carry only the data that belongs to that case. Use Playing(int TurnsTaken), Won(int TurnsTaken), and Lost(string Reason) as examples. 
```

# Connecting Blazor UI events to game logic

```text
Please explain what @bind does in a Blazor input element and why @bind="oninput" updates a field as the user types.
```

```text
Please explain why @onsubmit="true" is needed when a Blazor form should submit a game action instead of causing a normal browser page reload.
```

# Building a shared game status and command bar

```text
Please explain how a Blazor parent component can receive information from a child component using EventCallback. Use a GameCommandBar component that raises PuzzleAction values. And explain the difference between component parameters that pass data into a child component and event callbacks that notify the parent component.
```

```text
Review this game design: GameStatusBar displays state, GameCommandBar raises player actions, the page owns the current model, and game rules update the model. Explain why this separation is useful.
```
